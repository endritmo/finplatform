apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: finplatform
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP    # Internal only; Ingress routes external traffic
